# Використовуємо офіційний образ Node.js версії 18
FROM node:18-alpine

# Встановлюємо робочу директорію всередині контейнера
WORKDIR /usr/src/app

# Копіюємо package.json та package-lock.json для встановлення залежностей
COPY package*.json ./

# Встановлюємо залежності проекту
RUN npm install

# Копіюємо решту файлів проекту в робочу директорію
COPY . .

# Відкриваємо порт 3000, на якому працює наш сервер
EXPOSE 3000

# Вказуємо команду для запуску сервера при старті контейнера
CMD [ "node", "server.js" ]